#include"monty.h"
/**
 * f_stack -  sets the format of the data
 * Description: to a stack (LIFO)
 * @head: stack head
 * @counter: line_number
 * Return: void
 */
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi;

	bus.lifi = 0;
}
